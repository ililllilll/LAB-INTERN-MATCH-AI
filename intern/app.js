const CATEGORY_SUMMARY = [{"query_category": "전체 추천", "matched_records": 121, "p1_count": 56, "p2_count": 19, "p3_count": 46, "best_recruitment_months": "2025년 12월, 2026년 3월, 2026년 5월, 2026년 6월"}, {"query_category": "DGIST 전용/학교추천", "matched_records": 2, "p1_count": 2, "p2_count": 0, "p3_count": 0, "best_recruitment_months": "2026년 1월, 2026년 7월, 2025년 11월, 2026년 5월"}, {"query_category": "삼성그룹 대학생인턴", "matched_records": 11, "p1_count": 11, "p2_count": 0, "p3_count": 0, "best_recruitment_months": "2026년 3월"}, {"query_category": "반도체", "matched_records": 16, "p1_count": 8, "p2_count": 3, "p3_count": 5, "best_recruitment_months": "2026년 6월, 2026년 4월, 2026년 3월, 2025년 12월"}, {"query_category": "배터리", "matched_records": 14, "p1_count": 8, "p2_count": 1, "p3_count": 5, "best_recruitment_months": "2026년 5월, 2026년 3월, 2025년 11월, 2026년 2월"}, {"query_category": "디스플레이/전자부품", "matched_records": 5, "p1_count": 4, "p2_count": 1, "p3_count": 0, "best_recruitment_months": "2026년 3월, 2026년 2월, 2025년 11월, 2026년 5월"}, {"query_category": "방산/항공우주", "matched_records": 9, "p1_count": 5, "p2_count": 1, "p3_count": 3, "best_recruitment_months": "2025년 12월, 2026년 3월, 2026년 5월, 2026년 2월"}, {"query_category": "건축/건설/인프라", "matched_records": 6, "p1_count": 2, "p2_count": 2, "p3_count": 2, "best_recruitment_months": "2026년 3월, 2026년 1월, 2026년 2월, 2025년 11월"}, {"query_category": "조선/해양", "matched_records": 4, "p1_count": 2, "p2_count": 1, "p3_count": 1, "best_recruitment_months": "2026년 3월, 2026년 7월, 2025년 9월, 2025년 11월"}, {"query_category": "물류/SCM", "matched_records": 10, "p1_count": 6, "p2_count": 1, "p3_count": 3, "best_recruitment_months": "2026년 6월, 2025년 12월, 2025년 11월, 2026년 1월"}, {"query_category": "자동차/모빌리티", "matched_records": 17, "p1_count": 7, "p2_count": 3, "p3_count": 7, "best_recruitment_months": "2026년 5월, 2026년 4월, 2026년 7월, 2026년 1월"}, {"query_category": "기계/설계/제조", "matched_records": 17, "p1_count": 12, "p2_count": 2, "p3_count": 3, "best_recruitment_months": "2025년 12월, 2026년 3월, 2026년 5월, 2026년 4월"}, {"query_category": "화학/소재", "matched_records": 27, "p1_count": 18, "p2_count": 2, "p3_count": 7, "best_recruitment_months": "2026년 3월, 2026년 5월, 2025년 7월, 2025년 11월"}, {"query_category": "바이오/제약", "matched_records": 14, "p1_count": 9, "p2_count": 1, "p3_count": 4, "best_recruitment_months": "2026년 3월, 2025년 7월, 2025년 11월, 2026년 1월"}, {"query_category": "제조/생산/품질", "matched_records": 76, "p1_count": 42, "p2_count": 8, "p3_count": 26, "best_recruitment_months": "2025년 12월, 2026년 3월, 2026년 5월, 2026년 6월"}, {"query_category": "IT/소프트웨어/데이터/AI", "matched_records": 50, "p1_count": 15, "p2_count": 11, "p3_count": 24, "best_recruitment_months": "2026년 3월, 2026년 5월, 2026년 4월, 2026년 6월"}, {"query_category": "에너지/발전", "matched_records": 5, "p1_count": 2, "p2_count": 1, "p3_count": 2, "best_recruitment_months": "2026년 5월, 2026년 6월, 2026년 4월, 2025년 11월"}, {"query_category": "공공기관/공기업", "matched_records": 8, "p1_count": 1, "p2_count": 5, "p3_count": 2, "best_recruitment_months": "2026년 1월, 2026년 2월, 2026년 5월, 2026년 7월"}, {"query_category": "금융/디지털IT", "matched_records": 6, "p1_count": 1, "p2_count": 3, "p3_count": 2, "best_recruitment_months": "2026년 6월, 2026년 5월, 2026년 7월, 상시/채용시마감"}, {"query_category": "식품/소비재 제조", "matched_records": 10, "p1_count": 3, "p2_count": 4, "p3_count": 3, "best_recruitment_months": "2026년 6월, 2026년 4월, 2026년 5월, 2026년 2월"}, {"query_category": "외국계/컨설팅", "matched_records": 18, "p1_count": 4, "p2_count": 3, "p3_count": 11, "best_recruitment_months": "2025년 12월, 2026년 6월, 2026년 4월, 2025년 11월"}, {"query_category": "상사/무역/영업", "matched_records": 1, "p1_count": 1, "p2_count": 0, "p3_count": 0, "best_recruitment_months": "2026년 3월"}, {"query_category": "마케팅/광고/콘텐츠", "matched_records": 1, "p1_count": 1, "p2_count": 0, "p3_count": 0, "best_recruitment_months": "2026년 3월"}];
    const DB_STATS = {"total_records": 121, "period": "기본 수집범위 2025년 11월~2026년 6월, 삼성그룹 2026년 3월 상세공고 보강, DGIST 세메스/CUop 통합 포함, 출시 전 점검 보정 적용", "ranking": "career_rank_v10_release_checked"};

    const state = {
      selectedCategories: ['반도체'], query: '', queryRaw: '', intent: 'general', sort: 'recommended', showReview: false,
      season: 'all', fit: 'all', source: 'all'
    };

    const categoryIcons = {
      '전체 추천':'⭐','DGIST 전용/학교추천':'🎓','삼성그룹 대학생인턴':'🟦','반도체':'🧩','배터리':'🔋','디스플레이/전자부품':'💡','방산/항공우주':'🚀','건축/건설/인프라':'🏗️','조선/해양':'🚢','물류/SCM':'📦',
      '자동차/모빌리티':'🚗','기계/설계/제조':'⚙️','화학/소재':'⚗️','바이오/제약':'🧬','제조/생산/품질':'🏭','IT/소프트웨어/데이터/AI':'💻','에너지/발전':'⚡',
      '공공기관/공기업':'🏛️','금융/디지털IT':'💳','식품/소비재 제조':'🥫','외국계/컨설팅':'🌐','상사/무역/영업':'🌏','마케팅/광고/콘텐츠':'🎨'
    };

    const keywordCategory = [
      [/(디지스트|dgist|학교추천|장기현장실습|현장실습|cuop|산학협력|중소기업 인턴)/i, 'DGIST 전용/학교추천'],
      [/(외국계.*(반도체|장비)|반도체.*외국계|글로벌.*장비|lam|램리서치|kla|amat|applied|어플라이드|도쿄일렉트론|tel|온세미|onsemi|캐논쎄미콘덕터|인테그리스)/i, '외국계/컨설팅'],
      [/(삼성그룹|samsung group|제일기획|삼성전자|삼성디스플레이|삼성sds|삼성물산|삼성e&a|삼성중공업|삼성전기|삼성바이오로직스|삼성sdi|삼성 인턴)/i, '삼성그룹 대학생인턴'],
      [/(금융|은행|투자|핀테크|디지털금융)/i, '금융/디지털IT'],
      [/(식품|소비재|주류|음료|야쿠르트|오비맥주|하이트진로|피앤지|p&g|로레알|유니레버)/i, '식품/소비재 제조'],
      [/(qa|qc|quality|품질관리|품질보증|품질|gmp)/i, '제조/생산/품질'],
      [/(환경안전|안전환경|ehs|안전관리|보건관리|환경관리|안전보건)/i, '제조/생산/품질'],
      [/(로봇|로보틱스|자동화)/i, '기계/설계/제조'],
      [/(r&d|연구개발|연구직|개발연구)/i, '전체 추천'],
      [/(설비기술|설비엔지니어|장비엔지니어|field service|fse|cs엔지니어)/i, '기계/설계/제조'],
      [/(공기업.*기술|기술직.*공기업|공공.*기술)/i, '공공기관/공기업'],
      [/(생명과학|생명공학|바이오|제약|의약|제형|배양|정제|bio|pharma)/i, '바이오/제약'],
      [/(화공|화학공학|화학|소재|재료|고분자|케미칼|정밀화학|필름|프리커서|slurry|슬러리)/i, '화학/소재'],
      [/(기계공학|기계|기구설계|기계설계|제어|cae|구조해석)/i, '기계/설계/제조'],
      [/(반도체|fab|파운드리|메모리|노광|etch|depo|semiconductor|asml|하이닉스|세메스|semes)/i, '반도체'],
      [/(배터리|전지|이차전지|전동화|양극재|음극재|battery|리튬)/i, '배터리'],
      [/(디스플레이|oled|led|전자부품|display|lx세미콘|삼성디스플레이|삼성전기)/i, '디스플레이/전자부품'],
      [/(방산|항공|우주|미사일|유도무기|레이더|kai|넥스원|aerospace|defense)/i, '방산/항공우주'],
      [/(건축|건설|토목|인프라|플랜트|시공)/i, '건축/건설/인프라'],
      [/(조선|선박|해양플랜트|해양|오션|중공업)/i, '조선/해양'],
      [/(해운|물류|scm|logistics|로지스|글로비스|공항)/i, '물류/SCM'],
      [/(자동차|모빌리티|차량|전장|현대차|모비스|케피코|오토)/i, '자동차/모빌리티'],
      [/(공정관리|생산|제조|공장|생산기술)/i, '제조/생산/품질'],
      [/(it|소프트웨어|데이터|ai|인공지능|클라우드|보안|sw|프론트|백엔드|sds|안랩|네이버|카카오)/i, 'IT/소프트웨어/데이터/AI'],
      [/(에너지|발전|전력|가스|수소|태양광|서부발전|e1)/i, '에너지/발전'],
      [/(공공|공기업|기관|도로공사|통계)/i, '공공기관/공기업'],
      [/(금융|은행|투자|핀테크|디지털금융)/i, '금융/디지털IT'],
      [/(식품|소비재|주류|음료|야쿠르트|오비맥주|하이트진로|피앤지|p&g|로레알|유니레버)/i, '식품/소비재 제조'],
      [/(상사|무역|트레이딩|기술영업|해외영업)/i, '상사/무역/영업'],
      [/(마케팅|광고|콘텐츠|제일기획|브랜드|미디어)/i, '마케팅/광고/콘텐츠']
    ];

    function esc(v) {
      return String(v ?? '').replace(/[&<>'"]/g, s => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[s]));
    }
    function splitSemi(v) { return String(v || '').split(/[;,]/).map(x=>x.trim()).filter(Boolean); }
    function hasCategory(row, cat) { return cat === '전체 추천' ? true : (row._category_list || []).includes(cat); }
    function activeCategories() { return state.selectedCategories.length ? state.selectedCategories : ['전체 추천']; }
    function isAllSelected() { return activeCategories().includes('전체 추천'); }
    function rowMatchesSelectedCategories(row) { return isAllSelected() || activeCategories().some(cat => hasCategory(row, cat)); }
    function matchedCategoryDetails(row) {
      return activeCategories().filter(cat => cat !== '전체 추천').map(cat => ({cat, score: categoryFitScore(row, cat)})).filter(x => x.score > 0);
    }
    function multiCategoryFitScore(row) {
      const scores = matchedCategoryDetails(row).map(x => x.score).sort((a,b)=>b-a);
      if(!scores.length) return 0;
      const base = scores[0];
      const extra = Math.min(120, scores.slice(1).reduce((sum, score) => sum + (score >= 360 ? 75 : 25), 0));
      return base + extra;
    }
    function multiCompanySpecificBonus(row) {
      if(isAllSelected()) return companySpecificBonus(row, '전체 추천');
      const bonuses = activeCategories().map(cat => companySpecificBonus(row, cat)).sort((a,b)=>b-a);
      const best = bonuses[0] || 0;
      const extraPositive = Math.min(180, bonuses.slice(1).filter(v=>v>0).reduce((s,v)=>s+Math.min(v,90),0));
      const negative = bonuses.filter(v=>v<0).reduce((s,v)=>s+v,0);
      return best + extraPositive + negative;
    }
    function selectedCategoryLabel() {
      if(isAllSelected()) return `${categoryIcons['전체 추천']} 전체 추천`;
      return activeCategories().map(cat => `${categoryIcons[cat]||'📌'} ${cat}`).join(' + ');
    }
    function isMvp(row) { return row.expose_in_mvp === 'YES'; }
    function quality(row) { return Number(row.data_quality_score_v2 || 0); }
    function numberVal(v) { const n = Number(v); return Number.isFinite(n) ? n : 0; }
    function dateList(v) { return String(v||'').match(/\d{4}-\d{2}-\d{2}/g) || []; }
    function firstDate(v) { const d = dateList(v); return d.length ? d.sort()[0] : ''; }
    function exactDate(v) { return dateList(v).length > 0; }
    function categoryFitScore(row, cat) {
      if(cat === '전체 추천') return 0;
      const primary = row.primary_category || '';
      const cats = Array.isArray(row._category_list) ? row._category_list : splitSemi(row.query_category_aliases).concat(splitSemi(row.secondary_categories));
      if(primary === cat) return 360;
      if(cats.includes(cat)) return 35;
      return 0;
    }
    function conversionRankScore(row) {
      const t = [row.conversion_type,row.conversion_raw,row.internship_name].join(' ');
      if(/채용연계|전환|전제|장학전환/.test(t)) return 80;
      if(/우대|면접|가점/.test(t)) return 45;
      if(/체험형|비연계|Summer Associate|Internship/i.test(t)) return 18;
      return 30;
    }
    function fourthYearRankScore(row) {
      const t = [row.fourth_year_fit_label,row.eligibility_for_4th_year_clean,row.degree_requirement_clean,row.target_student_type,row.master_fit_label].join(' ');
      if(/4학년 추천 가능/.test(t)) return 60;
      if(/4학년 조건부|졸업예정|재학생/.test(t)) return 42;
      if(/비권장|석사 필수|석사 대상/.test(t)) return -30;
      if(/원문확인/.test(t)) return -10;
      return 0;
    }
    function deadlineRankScore(row) {
      const t = [row.application_deadline,row.application_period_raw,row.deadline_quality].join(' ');
      if(dateList(t).length > 0) return 40;
      if(/상시|채용 시|채용시/.test(t)) return 0;
      if(/원문|확인/.test(t)) return -25;
      return 5;
    }
    function priorityRankScore(row) {
      if(row.recommendation_priority === 'P1_우선추천') return 50;
      if(row.recommendation_priority === 'P2_일반추천') return 20;
      if(row.recommendation_priority === 'P3_내부검색/검수후노출') return -35;
      return 0;
    }

    const samsungOrder = {'삼성전자 DS':1,'삼성전자 DX':2,'삼성디스플레이':3,'삼성SDI':4,'삼성전기':5,'삼성SDS':6,'삼성바이오로직스':7,'삼성중공업':8,'삼성E&A':9,'삼성물산 상사부문':10,'제일기획':11};
    function companySpecificBonus(row, cat) {
      const c = row.company || '', n = row.internship_name || '', p = row.primary_category || '';
      let b = 0;
      if(cat === '삼성그룹 대학생인턴') return 5000 - (samsungOrder[c] || 99) * 80;
      if(cat === '전체 추천') {
        if(c === '삼성전자 DS') b += 260;
        if(/SK하이닉스/.test(c)) b += 230;
        if(/현대자동차|현대모비스|삼성SDI|삼성바이오로직스|삼성디스플레이|한화에어로스페이스/.test(c)) b += 170;
        if(isCuopCard(row)) b -= 70;
      }
      if(cat === '반도체' && c === '삼성전자 DS') b += 750;
      if(cat === '반도체' && /SK하이닉스/.test(c)) b += 520;
      if(cat === '반도체' && c === '세메스(SEMES)') b += 260;
      if(cat === '반도체' && /램리서치|KLA|어플라이드|Applied|캐논쎄미콘덕터|인테그리스|온세미|도쿄일렉트론/i.test(c+n)) b += 160;
      if(cat === '방산/항공우주' && c === '한화에어로스페이스') {
        if(/상반기 대규모/.test(n)) b += 130;
        if(/LS사업부 현장 전문직/.test(n)) b -= 80;
      }
      if(cat === '바이오/제약' && c === 'GC녹십자') b += 90;
      if(cat === '바이오/제약' && c === '한국콜마') b -= 35;
      if(cat === '에너지/발전') {
        if(/한국서부발전/.test(c)) b += 260;
        if(/E1/.test(c)) b += 220;
        if(p !== '에너지/발전') b -= 90;
      }
      if(cat === '외국계/컨설팅' && /램리서치|KLA|어플라이드|Applied|도쿄일렉트론|온세미|캐논쎄미콘덕터|인테그리스|헨켈|유니레버|필립모리스|존슨앤드존슨|사노피|테슬라|알파사이츠|로레알|P&G|피앤지|한국피앤지/i.test(c+n)) b += 250;
      if(cat === '외국계/컨설팅' && /램리서치|KLA|어플라이드|Applied|도쿄일렉트론|온세미|캐논쎄미콘덕터|인테그리스/i.test(c+n)) b += 650;
      if(cat === '제조/생산/품질' && /삼성전자|현대자동차|현대모비스|한화에어로스페이스|삼성중공업|삼성SDI|세메스|삼성전기|삼성디스플레이|포스코/.test(c)) b += 360;
      if(cat === '제조/생산/품질' && /LS사업부 현장 전문직/.test(n)) b -= 180;
      if(cat === '자동차/모빌리티' && c === '현대자동차') b += 520;
      if(cat === '자동차/모빌리티' && c === '현대모비스') b += 480;
      if(cat === '자동차/모빌리티' && c === '현대케피코') b += 390;
      if(cat === '자동차/모빌리티' && c === '현대글로비스') b += 190;
      if(cat === '자동차/모빌리티' && c === '한국공항') b -= 260;
      if(cat === '기계/설계/제조') {
        if(c === '세메스(SEMES)') b += 540;
        if(c === 'LIG넥스원') b += 360;
        if(c === '삼성전자 DS') b += 210;
        if(/현대자동차|현대모비스|삼성중공업|한화에어로스페이스|한온시스템|현대케피코|삼성E&A/.test(c)) b += 260;
        if(/LS사업부 현장 전문직/.test(n)) b -= 220;
      }
      if(cat === '물류/SCM') {
        if(c === '현대글로비스') b += 360;
        if(c === '롯데글로벌로지스') b += 220;
        if(c === '한국공항') b -= 160;
      }
      if(cat === '금융/디지털IT') {
        if(c === '삼성물산 상사부문') b -= 900;
        if(/한국산업은행|IBK기업은행|카카오인베스트먼트/.test(c)) b += 180;
      }
      if(cat === '식품/소비재 제조') {
        if(/hy 한국야쿠르트|오비맥주|하이트진로|크라운제과/.test(c)) b += 360;
        if(/로레알|피앤지|P&G|유니레버/i.test(c+n)) b -= 260;
      }
      if(cat === '공공기관/공기업') {
        if(/한국서부발전|한국도로공사|한국토지주택공사|LH/.test(c)) b += 130;
      }
      return b;
    }
    function rowBlob(row) {
      return [row.company,row.internship_name,row.primary_category,row.secondary_categories,row.job_tags,row.search_keywords,row.eligibility_summary,row.location,row.degree_requirement_clean,row.master_requirement_clean].join(' ');
    }
    function intentSpecificBonus(row) {
      const intent = state.intent || 'general';
      const blob = rowBlob(row), c = row.company || '', p = row.primary_category || '';
      let b = 0;
      if(intent === 'quality') {
        if(/QA|QC|품질|Quality|GMP/i.test(blob)) b += 950;
        if(/영업|마케팅|콘텐츠/.test(blob)) b -= 160;
      } else if(intent === 'ehs') {
        if(/안전|환경|EHS|보건|Safety/i.test(blob)) b += 1150;
        else b -= 700;
      } else if(intent === 'robotics') {
        if(/로봇|로보틱스|자동화|제어|기계|기구|설비|장비|전장|모빌리티/i.test(blob)) b += 500;
        if(/기계\/설계\/제조|자동차\/모빌리티|반도체|방산\/항공우주/.test(p)) b += 140;
      } else if(intent === 'rnd') {
        if(/R&D|연구개발|연구|개발|설계|공정개발|회로설계|해양설계|기구개발/i.test(blob)) b += 760;
        if(p === 'IT/소프트웨어/데이터/AI' && !/삼성전자 DX|삼성SDS/.test(c)) b -= 260;
        if(/카카오인베스트먼트|스노우|네이버제트/.test(c)) b -= 500;
      } else if(intent === 'equipment') {
        if(/설비|장비|Infra|인프라|FSE|Field Service|기구|기계|보전|CS엔지니어|공정기술/i.test(blob)) b += 900;
        if(/삼성전자 DS|세메스|램리서치|KLA|어플라이드|Applied|도쿄일렉트론/i.test(c+blob)) b += 300;
      } else if(intent === 'publicTech') {
        if(hasCategory(row,'공공기관/공기업')) b += 400;
        if(/한국서부발전|한국도로공사|한국토지주택공사|LH/.test(c)) b += 550;
        if(/IBK|산업은행|은행/.test(c)) b -= 600;
      } else if(intent === 'finance') {
        if(/은행|금융|투자|디지털|데이터|IT/.test(blob)) b += 350;
        if(c === '삼성물산 상사부문') b -= 900;
      }
      return b;
    }
    function intentMatch(row) {
      const intent = state.intent || 'general';
      const blob = rowBlob(row), c = row.company || '';
      if(['general','seasonOnly','finance'].includes(intent)) return true;
      if(intent === 'quality') return /QA|QC|품질|Quality|GMP/i.test(blob);
      if(intent === 'ehs') return /안전|환경|EHS|보건|Safety/i.test(blob);
      if(intent === 'robotics') return /로봇|로보틱스|자동화|제어|기계|기구|설비|장비|전장|모빌리티/i.test(blob);
      if(intent === 'rnd') return /R&D|연구개발|연구|개발|설계|공정개발|회로설계|해양설계|기구개발/i.test(blob);
      if(intent === 'equipment') return /설비|장비|Infra|인프라|FSE|Field Service|기구|기계|보전|CS엔지니어|공정기술/i.test(blob);
      if(intent === 'publicTech') return hasCategory(row,'공공기관/공기업') && !/IBK|산업은행|은행/.test(c);
      return true;
    }
    function careerRankScore(row) {
      return numberVal(row.company_tier_score) * 12
        + multiCategoryFitScore(row)
        + conversionRankScore(row)
        + fourthYearRankScore(row)
        + deadlineRankScore(row)
        + quality(row) * 1.5
        + priorityRankScore(row)
        + multiCompanySpecificBonus(row)
        + intentSpecificBonus(row);
    }
    function rankReason(row) {
      if(row.record_id === 'DGIST-CUOP-2025-2026') return 'DGIST CUop 통합 안내 · 하계/동계 한 카드 묶음 · 직접 기업 추천을 밀어내지 않는 보조 선택지 · 학부생 대상 · 모집기간 명확';
      const bits = [];
      if(row.company_tier_grade) bits.push(`${row.company_tier_grade}급 ${row.company_tier_label || '기업 선호도'}`);
      const matches = matchedCategoryDetails(row);
      const direct = matches.filter(x => x.score >= 360).map(x => x.cat);
      const secondary = matches.filter(x => x.score >= 35 && x.score < 360).map(x => x.cat);
      if(matches.length > 1) bits.push(`${matches.map(x=>x.cat).join(' 및 ')} 복수 분야 관련`);
      else if(direct.length) bits.push(`${direct[0]} 직접 관련`);
      else if(secondary.length) bits.push(`${secondary[0]} 보조 관련`);
      if(state.intent && state.intent !== 'general') bits.push('질문 키워드 반영');
      if(conversionRankScore(row) >= 80) bits.push('채용연계/전환형');
      if(fourthYearRankScore(row) >= 40) bits.push('4학년 적합');
      if(deadlineRankScore(row) >= 40) bits.push('마감일 명확');
      return bits.slice(0,5).join(' · ');
    }
    function priorityLabel(row) {
      if(row.recommendation_priority === 'P1_우선추천') return ['P1 우선추천',''];
      if(row.recommendation_priority === 'P2_일반추천') return ['P2 일반추천','p2'];
      return ['검수 후 노출','p3'];
    }
    function deadlineText(row) {
      if(row.application_deadline) return row.application_deadline;
      if(row.application_period_raw) return row.application_period_raw;
      return '원문 확인';
    }
    function isCuopCard(row) {
      return row.record_id === 'DGIST-CUOP-2025-2026' || String(row.company||'').toLowerCase() === 'cuop';
    }
    function applyCuopFixedRank(rows) {
      if(state.sort !== 'recommended') return rows;
      const idx = rows.findIndex(isCuopCard);
      if(idx < 0) return rows;
      const cuop = rows.splice(idx, 1)[0];
      let insertAt = rows.length;
      if(activeCategories().length === 1 && activeCategories()[0] === 'DGIST 전용/학교추천') insertAt = Math.min(1, rows.length);
      else insertAt = Math.min(5, rows.length);
      rows.splice(insertAt, 0, cuop);
      return rows;
    }
    function dedupeRecordRows(rows) {
      const seen = new Set();
      return rows.filter(r => {
        const key = String(r.unified_record_id || r.dedupe_key || r.record_id || `${r.company}|${r.internship_name}`).trim();
        if(seen.has(key)) return false;
        seen.add(key);
        return true;
      });
    }
    function dedupeCompanyRows(rows) {
      const seen = new Set();
      const out = [];
      for(const r of rows) {
        const key = String(r.company || '').trim();
        if(!key || !seen.has(key)) {
          if(key) seen.add(key);
          out.push(r);
        }
      }
      return out;
    }
    function seasonMatch(row) {
      const raw = [row.internship_season_clean,row.internship_timing_estimate,row.internship_period_raw,row.recruitment_window_label].join(' ');
      if(state.season === 'all') return true;
      if(state.season === 'summer') return /(하계|여름|7|8|summer)/i.test(raw);
      if(state.season === 'winter') return /(동계|겨울|1|2|winter)/i.test(raw);
      if(state.season === 'unknown') return true;
      return true;
    }
    function fitMatch(row) {
      const text = [row.fourth_year_fit_label,row.master_fit_label,row.eligibility_for_4th_year_clean,row.target_student_type,row.degree_requirement_clean,row.master_requirement_clean,row.eligibility_summary].join(' ');
      if(state.fit === 'all') return true;
      if(state.fit === 'fourth') return /(4학년 추천 가능|4학년 조건부|졸업예정|재학생)/.test(text) && !/(단독지원 비권장|석사 필수|석사 대상)/.test(text);
      if(state.fit === 'undergrad') return !/(석사 중심|석사 필수|석사 대상|석사 제외)/.test(text);
      if(state.fit === 'master') return /(석사 지원 가능|학사\/석사|학부\/석사|석사 졸업|석사 채용|석사 가능|학\/석사)/.test(text) && !/(석사 제외|학부생 대상, 석사 대상 아님|석사 대상 아님)/.test(text);
      if(state.fit === 'excludeMasterOnly') return !/(석사 중심|석사 필수|석사 대상|단독지원 비권장)/.test(text);
      return true;
    }
    function sourceMatch(row) { return state.source === 'all' || row.source_db === state.source; }
    function textMatch(row) {
      const q = state.query.trim().toLowerCase();
      if(!q) return true;
      const stop = new Set(['인턴','추천','알려줘','찾아줘','공고','기업','분야','관련','해주세요','해줘']);
      const tokens = q.split(/\s+/).map(t=>t.trim()).filter(t => t && !stop.has(t));
      if(!tokens.length) return true;
      const blob = [row.company,row.internship_name,row.primary_category,row.secondary_categories,row.job_tags,row.search_keywords,row.eligibility_summary,row.location].join(' ').toLowerCase();
      return tokens.some(t => blob.includes(t));
    }
    function filteredRows() {
      let rows = INTERN_DATA.filter(rowMatchesSelectedCategories);
      if(!state.showReview) rows = rows.filter(isMvp);
      rows = rows.filter(seasonMatch).filter(fitMatch).filter(sourceMatch).filter(intentMatch).filter(textMatch);
      rows = dedupeRecordRows(rows);
      rows.sort((a,b) => {
        const ar = careerRankScore(a), br = careerRankScore(b);
        if(state.sort === 'deadline') {
          const ad = firstDate([a.application_deadline,a.application_period_raw].join(' ')) || '9999-99-99';
          const bd = firstDate([b.application_deadline,b.application_period_raw].join(' ')) || '9999-99-99';
          return ad.localeCompare(bd) || (br - ar) || quality(b)-quality(a);
        }
        if(state.sort === 'quality') return quality(b)-quality(a) || (br - ar);
        return (br - ar) || quality(b)-quality(a);
      });
      rows = dedupeCompanyRows(rows);
      rows = applyCuopFixedRank(rows);
      if(activeCategories().length === 1 && activeCategories()[0] === '자동차/모빌리티' && !state.showReview) rows = rows.slice(0,7);
      return rows;
    }
    function displayedCountForCategory(cat) {
      const prevCategories = [...state.selectedCategories];
      try {
        state.selectedCategories = [cat];
        return filteredRows().length;
      } finally {
        state.selectedCategories = prevCategories;
      }
    }
    function inferCategories(q) {
      const text = String(q||'').trim();
      if(!text) return [];
      const found = [];
      const add = cat => { if(cat && cat !== '전체 추천' && !found.includes(cat)) found.push(cat); };
      CATEGORY_SUMMARY.forEach(c => { if(c.query_category !== '전체 추천' && text.includes(c.query_category)) add(c.query_category); });
      for(const [re, cat] of keywordCategory) {
        re.lastIndex = 0;
        if(re.test(text)) add(cat);
      }
      if(/전체 추천|전체 분야|모든 분야|전부/.test(text) && !found.length) return ['전체 추천'];
      return found;
    }
    function inferIntent(q) {
      const text = String(q||'');
      if(/공기업.*기술|기술직.*공기업|공공.*기술/.test(text)) return 'publicTech';
      if(/QA|QC|quality|품질관리|품질보증|품질|GMP/i.test(text)) return 'quality';
      if(/환경안전|안전환경|EHS|안전관리|보건관리|환경관리|안전보건/i.test(text)) return 'ehs';
      if(/로봇|로보틱스|자동화/i.test(text)) return 'robotics';
      if(/R&D|연구개발|연구직|개발연구/i.test(text)) return 'rnd';
      if(/설비기술|설비엔지니어|장비엔지니어|field service|FSE|CS엔지니어/i.test(text)) return 'equipment';
      if(/금융|은행|투자|핀테크|디지털금융/i.test(text)) return 'finance';
      if(/하계|여름|동계|겨울/.test(text)) return 'seasonOnly';
      return 'general';
    }
    function applyQueryIntent(q) {
      const text = String(q||'').trim();
      state.queryRaw = text;
      state.query = '';
      const inferred = inferCategories(text);
      if(inferred.length) state.selectedCategories = inferred;
      state.intent = inferIntent(text);
      if(/검수|전체|전부|포함/.test(text)) state.showReview = true;
      state.season = /하계|여름|7월|8월|summer/i.test(text) ? 'summer' : (/동계|겨울|1월|2월|winter/i.test(text) ? 'winter' : 'all');
      if(/석사 제외|학부만|학부생/.test(text)) state.fit = 'excludeMasterOnly';
      else if(/석사/.test(text)) state.fit = 'master';
      else if(/4학년|졸업예정/.test(text)) state.fit = 'fourth';
      else state.fit = 'all';
    }
    function renderStats() {
      const statTotal = document.getElementById('statTotal');
      if(statTotal) statTotal.textContent = DB_STATS.total_records;
    }
    function renderCategories() {
      const box = document.getElementById('categoryChips');
      box.innerHTML = CATEGORY_SUMMARY.map(c => {
        const displayCount = displayedCountForCategory(c.query_category);
        const active = c.query_category === '전체 추천' ? isAllSelected() : (!isAllSelected() && activeCategories().includes(c.query_category));
        return `<span class="chip ${active?'active':''}" data-cat="${esc(c.query_category)}">${categoryIcons[c.query_category]||'📌'} ${esc(c.query_category)} <small>${displayCount}</small></span>`;
      }).join('');
      box.querySelectorAll('.chip').forEach(ch => ch.addEventListener('click', () => {
        const cat = ch.dataset.cat;
        if(cat === '전체 추천') {
          state.selectedCategories = ['전체 추천'];
        } else {
          const current = isAllSelected() ? [] : [...state.selectedCategories];
          state.selectedCategories = current.includes(cat) ? current.filter(x => x !== cat) : [...current, cat];
          if(!state.selectedCategories.length) state.selectedCategories = ['전체 추천'];
        }
        Object.assign(state, {query:'', queryRaw:'', intent:'general', season:'all', fit:'all', source:'all'});
        document.getElementById('searchInput').value='';
        document.getElementById('seasonFilter').value='all';
        document.getElementById('fitFilter').value='all';
        document.getElementById('sourceFilter').value='all';
        render();
      }));
    }
    function renderAnswer(rows) {
      const selected = activeCategories();
      const sums = selected.map(cat => CATEGORY_SUMMARY.find(c => c.query_category === cat)).filter(Boolean);
      const months = [...new Set(sums.flatMap(s => String(s.best_recruitment_months || '').split(/[,/]/).map(x=>x.trim()).filter(Boolean)))].slice(0,4).join(', ') || '데이터 부족';
      const visibleCount = rows.length;
      const sparse = visibleCount <= 3 && !isAllSelected() ? '<br/><small>선택 분야의 현재 축적 데이터가 적어 유사 분야와 공식 공고를 함께 확인하는 것이 안전합니다.</small>' : '';
      const intentText = state.intent && state.intent !== 'general' ? ` 질문 키워드 <b>${esc(state.queryRaw)}</b>를 반영했습니다.<br/>` : '';
      const countText = isAllSelected() ? `현재 필터 기준 ${visibleCount}건이 검색됩니다.` : `선택한 ${selected.length}개 분야 기준으로 현재 ${visibleCount}건이 검색됩니다.`;
      document.getElementById('answerBox').innerHTML = `
        <div>
          <h2>${esc(selectedCategoryLabel())} 인턴 추천</h2>
          <p>${intentText}작년 기준 주요 모집월: <b>${esc(months)}</b><br/>${countText}<br/><small>정렬 기준: 대기업/그룹사, 복수 분야 적합도, 채용연계성, 4학년/학위 적합도, 데이터 신뢰도, 질문 키워드.</small>${sparse}</p>
        </div>
        <div class="pill">전체 ${visibleCount}건</div>`;
    }
    function displayFourthYearLabel(row) {
      const raw = row.fourth_year_fit_label || row.eligibility_for_4th_year_clean || '';
      if(/단독\s*지원\s*비권장|단독지원\s*비권장|비권장/.test(raw)) return '원문 확인';
      return raw || '원문 확인';
    }
    function hideSourceUrl(row) {
      return ['DGIST-SEMES-2026','DGIST-CUOP-2025-2026'].includes(row.record_id) || !row.source_url;
    }
    function sourceActionHtml(row) {
      const copyText = hideSourceUrl(row) ? (row.company + ' ' + row.internship_name) : (row.company + ' ' + row.internship_name + ' ' + row.source_url);
      const copyBtn = `<button class="copy-btn" data-copy="${esc(copyText)}">복사</button>`;
      if(hideSourceUrl(row)) return copyBtn;
      return `<a class="link-btn" href="${esc(row.source_url)}" target="_blank" rel="noopener">공고 URL 보기</a>${copyBtn}`;
    }
    function sourceTableHtml(row) {
      if(hideSourceUrl(row)) return '-';
      return `<a href="${esc(row.source_url)}" target="_blank" rel="noopener">공고</a>`;
    }
    function sourceChatHtml(row) {
      if(hideSourceUrl(row)) return '';
      return ` / <a href="${esc(row.source_url)}" target="_blank" rel="noopener">공고 보기</a>`;
    }
    function tagHtml(row) {
      const tags = [];
      const degreeTagPattern = /(학사|석사|전문학사|학부생|학\/석사|학부\/석사|졸업예정|기졸업|학위)/;
      if(row.primary_category) tags.push(`<span class="tag">${esc(row.primary_category)}</span>`);
      splitSemi(row.job_tags)
        .filter(t => !degreeTagPattern.test(t))
        .slice(0,3)
        .forEach(t => tags.push(`<span class="tag gray">${esc(t)}</span>`));
      const fourthYearTag = displayFourthYearLabel(row);
      if(fourthYearTag && fourthYearTag !== '원문 확인') tags.push(`<span class="tag ${/원문/.test(fourthYearTag)?'warn':''}">${esc(fourthYearTag)}</span>`);
      return tags.join('');
    }
    function renderCards(rows) {
      const box = document.getElementById('cards');
      if(!rows.length) { box.innerHTML = `<div class="empty">조건에 맞는 인턴이 없습니다. 검수필요 포함 또는 다른 분야를 선택해 보세요.</div>`; return; }
      box.innerHTML = rows.map((r, idx) => {
        const [pl, pc] = priorityLabel(r);
        const notice = r.student_notice || r.operational_limitation_notes || '지원 전 원문 공고 확인 필요';
        return `<article class="card">
          <div class="card-head">
            <div style="display:flex;gap:10px;align-items:flex-start">
              <div class="rank">${idx+1}</div>
              <div><h3 class="company">${esc(r.company)}</h3><p class="internship">${esc(r.internship_name)}</p></div>
            </div>
            <div style="display:flex;gap:6px;align-items:center;flex-wrap:wrap;justify-content:flex-end"><span class="priority ${pc}">${esc(pl)}</span><span class="tier-badge">${esc(r.company_tier_grade || '-')} · ${esc(r.company_tier_label || '기업분류')}</span></div>
          </div>
          <div class="tags">${tagHtml(r)}</div>
          <div class="meta-grid">
            <div class="meta"><span>서류 마감</span><b>${esc(deadlineText(r))}</b></div>
            <div class="meta"><span>모집 시기</span><b>${esc(r.recruitment_month_label || r.application_period_raw || '원문 확인')}</b></div>
            <div class="meta"><span>인턴 분야</span><b>${esc(r.job_tags || r.primary_category || '원문 확인')}</b></div>
            <div class="meta"><span>근무/실습 시기</span><b>${esc(r.internship_timing_estimate || r.internship_period_raw || '원문 확인')}</b></div>
            <div class="meta"><span>서류 발표 예상</span><b>${esc(r.document_result_window_estimate || '원문 확인')}</b></div>
            <div class="meta"><span>면접 예상</span><b>${esc(r.interview_window_estimate || '원문 확인')}</b></div>
            <div class="meta"><span>4학년 대상 여부</span><b>${esc(displayFourthYearLabel(r))}</b></div>
            <div class="meta"><span>학위 조건</span><b>${esc(r.degree_requirement_clean || r.target_student_type || '원문 확인')}</b></div>
          </div>
          <div class="rank-note"><b>추천순 근거</b>: ${esc(rankReason(r))}</div>
          <div class="notice">${esc(notice)}${r.operational_limitation_notes ? ' · ' + esc(r.operational_limitation_notes) : ''}</div>
          <div class="actions">${sourceActionHtml(r)}</div>
        </article>`;
      }).join('');
      box.querySelectorAll('[data-copy]').forEach(btn => btn.addEventListener('click', async () => {
        try { await navigator.clipboard.writeText(btn.dataset.copy); btn.textContent='복사됨'; setTimeout(()=>btn.textContent='복사',1200); } catch(e) { alert(btn.dataset.copy); }
      }));
    }
    function renderTable(rows) {
      const wrap = document.getElementById('tableWrap');
      wrap.style.display = document.getElementById('tableToggle').checked ? 'block' : 'none';
      if(wrap.style.display === 'none') return;
      wrap.innerHTML = `<div class="table-scroll-hint">표는 좌우로 밀어서 확인할 수 있습니다.</div><table><thead><tr><th>순위</th><th>기업</th><th>인턴명</th><th>분야</th><th>서류마감</th><th>면접예상</th><th>실습예상</th><th>4학년</th><th>URL</th></tr></thead><tbody>` +
        rows.map((r,i)=>`<tr><td>${i+1}</td><td>${esc(r.company)}</td><td>${esc(r.internship_name)}</td><td>${esc(r.job_tags || r.primary_category)}</td><td>${esc(deadlineText(r))}</td><td>${esc(r.interview_window_estimate)}</td><td>${esc(r.internship_timing_estimate)}</td><td>${esc(displayFourthYearLabel(r))}</td><td>${sourceTableHtml(r)}</td></tr>`).join('') + `</tbody></table>`;
    }
    function render() {
      renderStats(); renderCategories();
      const rows = filteredRows();
      renderAnswer(rows); renderCards(rows); renderTable(rows);
    }
    function askChat(q) {
      const log = document.getElementById('chatLog');
      if(!q.trim()) return;
      log.innerHTML += `<div class="bubble user">${esc(q)}</div>`;
      applyQueryIntent(q);
      const rows = filteredRows().slice(0,5);
      const selected = activeCategories();
      const months = [...new Set(selected.map(cat => CATEGORY_SUMMARY.find(c => c.query_category === cat)?.best_recruitment_months).filter(Boolean))].join(', ') || '데이터 부족';
      let html = `<b>${esc(selectedCategoryLabel())}</b> 기준으로 찾았습니다. 작년 주요 모집월은 <b>${esc(months)}</b>입니다.<br/>`;
      if(state.intent && state.intent !== 'general') html += `질문 키워드 <b>${esc(state.queryRaw)}</b>를 반영해 정렬했습니다.<br/>`;
      if(rows.length) {
        html += rows.map((r,i)=>`${i+1}. <b>${esc(r.company)}</b>, ${esc(r.internship_name)}<br/>추천근거: ${esc(rankReason(r))}<br/>서류마감: ${esc(deadlineText(r))} / 면접예상: ${esc(r.interview_window_estimate || '원문 확인')} / 실습: ${esc(r.internship_timing_estimate || '원문 확인')}${sourceChatHtml(r)}`).join('<br/><br/>');
      } else {
        html += '현재 조건에 맞는 공고가 없습니다. 검수필요 포함을 켜거나 필터를 완화해 보세요.';
      }
      html += `<br/><br/><small>면접과 실습 일정은 실제 공고값이 없을 경우 추정값입니다. 향후 공고는 일정과 직무가 달라질 수 있으므로 지원 전 공식 채용공고와 URL의 최신 내용을 확인해 주세요.</small>`;
      log.innerHTML += `<div class="bubble bot">${html}</div>`;
      document.getElementById('chatLog').scrollTop = log.scrollHeight;
      document.getElementById('seasonFilter').value = state.season;
      document.getElementById('fitFilter').value = state.fit;
      document.getElementById('reviewToggle').checked = state.showReview;
      render();
    }

    document.getElementById('searchBtn').addEventListener('click', () => {
      const q = document.getElementById('searchInput').value.trim();
      applyQueryIntent(q);
      document.getElementById('seasonFilter').value = state.season;
      document.getElementById('fitFilter').value = state.fit;
      document.getElementById('reviewToggle').checked = state.showReview;
      render();
    });
    document.getElementById('searchInput').addEventListener('keydown', e => { if(e.key==='Enter') document.getElementById('searchBtn').click(); });
    document.getElementById('chatBtn').addEventListener('click', () => { const input=document.getElementById('chatInput'); askChat(input.value); input.value=''; });
    document.getElementById('chatInput').addEventListener('keydown', e => { if(e.key==='Enter') document.getElementById('chatBtn').click(); });
    document.getElementById('reviewToggle').addEventListener('change', e => { state.showReview=e.target.checked; render(); });
    document.getElementById('seasonFilter').addEventListener('change', e => { state.season=e.target.value; render(); });
    document.getElementById('fitFilter').addEventListener('change', e => { state.fit=e.target.value; render(); });
    document.getElementById('sourceFilter').addEventListener('change', e => { state.source=e.target.value; render(); });
    document.getElementById('tableToggle').addEventListener('change', render);
    document.querySelectorAll('.small-tab').forEach(t => t.addEventListener('click', () => {
      document.querySelectorAll('.small-tab').forEach(x=>x.classList.remove('active'));
      t.classList.add('active'); state.sort=t.dataset.sort; render();
    }));
    document.getElementById('resetBtn').addEventListener('click', () => {
      Object.assign(state, {selectedCategories:['반도체'],query:'',queryRaw:'',intent:'general',sort:'recommended',showReview:false,season:'all',fit:'all',source:'all'});
      document.getElementById('searchInput').value=''; document.getElementById('reviewToggle').checked=false; document.getElementById('seasonFilter').value='all'; document.getElementById('fitFilter').value='all'; document.getElementById('sourceFilter').value='all'; document.getElementById('tableToggle').checked=false;
      document.querySelectorAll('.small-tab').forEach(x=>x.classList.toggle('active',x.dataset.sort==='recommended'));
      render();
    });

    render();
